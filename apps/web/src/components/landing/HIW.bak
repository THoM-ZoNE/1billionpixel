export function HowItWorks() {
  const steps = [
    { n: "01", title: "Buy $1BPX", desc: "Purchase tokens on Pump.fun. 1 token = 1 pixel quota." },
    { n: "02", title: "Connect Wallet", desc: "Link your Solana wallet — Phantom, Backpack, or Solflare." },
    { n: "03", title: "Pick Your Spot", desc: "Select any free area on the 1 billion pixel canvas." },
    { n: "04", title: "Upload Your Art", desc: "Drop a JPG, PNG, or GIF. Your pixels go live instantly." },
  ];

  return (
    <section className="rounded-2xl border border-violet-700/40 bg-black/60 px-6 py-10 text-center shadow-[0_0_40px_rgba(139,92,246,0.35)]">
      <h2 className="mb-10 font-pixel text-lg tracking-[0.25em] text-slate-200">
        HOW IT WORKS
      </h2>

      <div className="grid gap-8 md:grid-cols-4">
        {steps.map((s) => (
          <div key={s.n} className="flex flex-col items-center gap-2">
            <div className="font-pixel text-3xl text-violet-400">{s.n}</div>
            <h3 className="text-base font-semibold text-slate-50">{s.title}</h3>
            <p className="max-w-[12rem] text-xs text-slate-400">{s.desc}</p>
          </div>
        ))}
      </div>
    </section>
  );
}
